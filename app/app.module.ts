import { NgModule } from '@angular/core';

import {
  bootstrap,
  declarations,
  imports,
  providers,
} from './config/config';

@NgModule({
  imports: imports,
  declarations: declarations,
  providers: providers,
  bootstrap: bootstrap
})
export class AppModule { }
